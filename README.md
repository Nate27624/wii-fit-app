# Wii Fit App

Android app for tracking Wii Fit Balance Board data.

## Requirements

- Android Studio Hedgehog or newer
- JDK 17
- Android SDK 34

## Run

```bash
./gradlew assembleDebug
```

Then install `app/build/outputs/apk/debug/app-debug.apk` on a device or emulator.
